
const arr = [1, 2, 3];
arr[1] = 6
arr.push(4) // mutating the array
console.log(arr);
arr.pop();
console.log(arr);
arr[0] = 'a string';
console.log(arr);

// const allows us to MUTATE an array or object.
// we cannot change it wholesale.

